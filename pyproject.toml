[project]
name = "pixie-qa-demo"
version = "0.1.0"
description = "Demo for using pixie-qa"
requires-python = ">=3.11"
dependencies = [
    "openai-agents>=0.6.5",
    "pydantic>=2.7.4",
]

[tool.uv]
package = false
